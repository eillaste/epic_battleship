@page
@model WebApplication.Pages_Games.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Game![0].CreationDT)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Game![0].PlayerA)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Game![0].PlayerB)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Game!) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CreationDT)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PlayerA)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PlayerB)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
